pipeline {
    agent any //where to execute 
    stages  { //where the work happens 
        stage("build"){
            steps {
                echo 'Building the application....'
            }
        }
    }
}
